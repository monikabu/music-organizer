
<div class="main_title">
  <p>Artist:</p>
</div>
<div>
  <h1><%=@artist.name%></h1>
</div>
<h3>Albums:</h3>
<%@artist.albums.each do |album|%>
  <p> <%=link_to album.title, artist_album_path(album.artist, album)%></p><br>
<%end%>
<div>
    <%= link_to "Create a new album", new_artist_album_path(@artist), :class => 'btn btn-lg btn-info'  %>
</div>